[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
# tests/     = ledger invariants, driven at service level as the DB owner
# tests_api/ = the whole REST surface, driven over ASGI as the app role
testpaths = tests tests_api
